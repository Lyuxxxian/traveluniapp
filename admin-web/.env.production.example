# 复制为 .env.production 后填入生产 API 地址（勿提交含真实域名的 .env.production 若含内网敏感信息）

# 管理后台请求的后端根地址（无尾斜杠）
#
# 【本地 preview 验收】请设为：
# VITE_API_BASE_URL=http://localhost:3000
# 或直接：npm run preview:local（使用 .env.preview，无需改本文件）
#
# 【生产 方案 A】同域 Nginx 反代 /api/：留空
# VITE_API_BASE_URL=
#
# 【生产 方案 B】API 独立域名：
# VITE_API_BASE_URL=https://api.your-domain.com
